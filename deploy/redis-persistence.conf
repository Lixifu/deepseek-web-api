# Merge these settings into the server's redis.conf.
# AOF keeps the distributed queue and rate-limit state durable to within ~1s.
appendonly yes
appendfsync everysec
aof-use-rdb-preamble yes

# Retain periodic RDB recovery points as a second layer.
save 900 1
save 300 10
save 60 10000
